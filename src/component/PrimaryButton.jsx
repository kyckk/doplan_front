import "../static/main.css"
const PrimaryButtonry = ({onClick} ) => {
    return (
       
        <button  onClick={onClick} className="primary-button">+</button>
    )
}

export default PrimaryButtonry;